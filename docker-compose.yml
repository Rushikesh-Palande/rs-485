services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: rs485
      MYSQL_USER: rs485
      MYSQL_PASSWORD: rs485
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    command: ["--default-authentication-plugin=mysql_native_password"]
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
