# Central place for constants to avoid magic strings all over the codebase.

API_PREFIX = "/api"
WS_REALTIME_PATH = "/ws/realtime"

# Header names used for correlation / tracing.
HDR_REQUEST_ID = "x-request-id"
HDR_CORRELATION_ID = "x-correlation-id"
